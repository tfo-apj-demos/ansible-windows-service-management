---
all:
  children:
    windows_servers:
      hosts:
        # Example Windows servers
        win-server-01:
          ansible_host: 192.168.1.100
          ansible_user: "{{ vault_windows_admin_user }}"
          ansible_password: "{{ vault_windows_admin_password }}"
          ansible_connection: winrm
          ansible_winrm_transport: basic
          ansible_winrm_server_cert_validation: ignore
          ansible_port: 5985
          
        win-server-02:
          ansible_host: 192.168.1.101
          ansible_user: "{{ vault_windows_admin_user }}"
          ansible_password: "{{ vault_windows_admin_password }}"
          ansible_connection: winrm
          ansible_winrm_transport: basic
          ansible_winrm_server_cert_validation: ignore
          ansible_port: 5985

    linux_servers:
      hosts:
        # Example Linux servers (if needed for management/monitoring)
        linux-mgmt-01:
          ansible_host: 192.168.1.200
          ansible_user: "{{ vault_linux_admin_user }}"
          ansible_ssh_private_key_file: "{{ vault_linux_ssh_key_path }}"
